<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <gradient
        android:endColor="@color/main_titlebar"
        android:startColor="@color/main_titlebar" />
    <!-- 矩形的内部颜色，这里设置成白色，即空心 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/main_titlebar" />

    <padding
        android:top="10dp"
        android:bottom="10dp"
        android:left="20dp"
        android:right="20dp" >
    </padding>
    <!--
    <size
        android:height="25dp"
        android:width="120dp" />


    -->
    <!-- 矩形的宽高 -->
    <corners android:radius="7dp" />
    <!-- 圆角半径 -->

</shape>